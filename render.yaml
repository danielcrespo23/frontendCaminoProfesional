services:
  - type: web
    name: camino-profesional
    runtime: static
    buildCommand: npm install && npm run build
    staticPublishPath: dist/camino-profesional/browser
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
